apiVersion: 1

datasources:
  - name: MySQL
    type: mysql
    access: proxy
    url: db:3306
    database: ${MYSQL_DATABASE}
    user: ${MYSQL_USER}
    secureJsonData:
      password: ${MYSQL_PASSWORD}
    isDefault: true
